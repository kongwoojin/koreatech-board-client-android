<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ide_navigation"
    app:startDestination="@id/navigation_ide_notice">

    <fragment
        android:id="@+id/navigation_ide_notice"
        android:name="com.kongjak.koreatechboard.ui.fragment.BoardFragment"
        android:label="@string/item_notice">

        <argument
            android:name="site"
            android:defaultValue="ide"
            app:argType="string" />

        <argument
            android:name="board"
            android:defaultValue="notice"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/navigation_ide_free_board"
        android:name="com.kongjak.koreatechboard.ui.fragment.BoardFragment"
        android:label="@string/item_free_board">

        <argument
            android:name="site"
            android:defaultValue="ide"
            app:argType="string" />

        <argument
            android:name="board"
            android:defaultValue="free"
            app:argType="string" />
    </fragment>
</navigation>